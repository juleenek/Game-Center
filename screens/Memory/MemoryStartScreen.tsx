import * as React from 'react';
import { View, Text } from 'react-native';

export const MemoryStartScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Memory Start Screen</Text>
    </View>
  );
};
